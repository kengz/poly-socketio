machine:
  node:
    version: 6

dependencies:
  override:
    - npm install

test:
  override:
    - npm test
  post:
    - cat ./coverage/lcov.info | ./node_modules/.bin/codeclimate-test-reporter

general:
  branches:
    ignore:
      - gh-pages
